import React from 'react'
import { useStyles } from './service.style'

const Service = () => {
    const classes = useStyles()
    return (
        <section id="#services">
            <h1 className={classes.servicesHeading}>This too is under construction...</h1>
        </section>
    )
}

export default Service
