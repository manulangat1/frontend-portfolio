import React from 'react'
import { useStyles } from './projects.style'

const Projects = () => {
    const classes = useStyles()
    return (
        <section id="/#projects" >
            <h1 className={classes.projectHeading}>Coming soon, under construction!</h1>
        </section>
    )
}

export default Projects
